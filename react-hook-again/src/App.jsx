/* react hook = a special function that allows 
                functional component to use react 
                features without writing class components
                (useState, useEffect, useReducer, useContext
                useCallback and more......)

  useState() = a react hook that allows creation of 
               stateful variable AND a setter to update 
               it's value in virtual DOM.  */
import MyComponent from './MyComponent.jsx'
function App() {
  
  return (
    <MyComponent/>
  )
}

export default App
